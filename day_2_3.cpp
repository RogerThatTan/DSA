#include <bits/stdc++.h>
using namespace std;
int main()
{

    // int age;
    // cout << "Enter your age : ";
    // cin >> age;
    // cout << age << endl;

    // int a = 1110;
    // int b = a++;
    // int c = --b;
    // cout << b << endl;
    // cout << a << endl;

    // Q1:

    // int n;
    // cout << "Input the side of the square: ";
    // cin >> n;
    // cout << "Area of the square is: " << (n * n) << endl;

    // Q2:
    // float pen, pencil, eraser, total, withgst;
    // cout << "Enter the price of pencil,pen,eraser: ";
    // cin >> pen >> pencil >> eraser;
    // total = (pen + pencil + eraser);
    // withgst = total + (total * 0.18);

    // cout << "Total Price without GST: " << total << endl;
    // cout << "Total Price with GST: " << withgst << endl;

    // Q3:
    //  float p, r, t;
    //  cout << "Input the principal,rate,time: ";
    //  cin >> p >> r >> t;
    //  float interest = (p + r + t) / 100;
    //  cout << "Interest is: " << interest << endl;

    // Q4:

    // float r;
    // cin >> r;
    // cout << "Area of the circle is : " << (3.14 * r * r) << endl;

    // return 0;
}